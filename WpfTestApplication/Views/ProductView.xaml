<Window 
    x:Class="WpfTestApplication.Views.ProductView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Converters="clr-namespace:WpfTestApplication.Converters"
    Title="ProductView" Height="300" Width="300"
    >

    <Window.Resources>
        <ResourceDictionary>
            <Converters:PriceFormatter x:Key="PriceFormatter"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Margin="3" Width="60" Height="80" BorderBrush="Black" BorderThickness="1" VerticalAlignment="Top" HorizontalAlignment="Left">
            <Image/>
        </Border>
        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
            <TextBlock Text="{Binding Item.Name}" Margin="3" Padding="2" TextWrapping="Wrap" FontWeight="Bold" FontSize="16" HorizontalAlignment="Right"/>
            <TextBlock Text="{Binding Item.ListPrice, Converter={StaticResource PriceFormatter}}" Margin="3" Padding="2" FontWeight="Bold" FontSize="12" HorizontalAlignment="Right"/>
        </StackPanel>
    </Grid>
</Window>
